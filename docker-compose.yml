version: '3.8'

services:
  xankong:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
